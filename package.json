{
  "name": "@introspection-recipes/pi-claude",
  "version": "0.1.1",
  "description": "Claude Code harness reproduced as an Introspection (Pi) recipe — a pi-claude starter template.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check": "npx -y -p @introspection-ai/pi-recipes@latest recipes check . --profile ci",
    "check:json": "npx -y -p @introspection-ai/pi-recipes@latest recipes check . --profile ci --json",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "peerDependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "agents": [
      "./agents/*.yaml"
    ],
    "skills": [
      "./skills/*/SKILL.md"
    ],
    "extensions": [
      "./extensions/*.ts"
    ]
  }
}
